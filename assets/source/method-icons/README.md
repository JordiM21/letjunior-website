# method-icons/ — the three icons in "Nuestro método"

**Replaces:** the 🎮 🎬 👩‍🏫 emoji in `#metodo`.
**How many:** 3.
**Send:** SVG if genuinely vector, otherwise PNG-24 with transparency at
**256px square**.

They sit in a 64px white rounded tile, so they read at thumbnail size: one
clear shape each, no fine detail, no text. Drawn as a set — same weight, same
perspective, same palette from the brand colours. A 3D-rendered set would suit
the Toca Boca side of the brand; flat vector is fine too, as long as all three
come from the same hand.

`icono-plataforma.png`, `icono-audiovisual.png`, `icono-clase-viva.png`
