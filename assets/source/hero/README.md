# hero/ — the main image at the top of the page

**Replaces:** the fake video-call mock in `index.html` (`.hero-visual`).
**How many:** 1.
**Send:** PNG-24 with transparency, **at least 1400px wide**.

This is the LCP element — the first thing a parent sees after a Meta ad click,
and the one image that is never lazy-loaded. It has to say "live class with a
real teacher and a few kids" in under a second, without being read.

Either a real screenshot of a live class (faces of consenting families only) or
an illustrated scene. Not a stock photo of a generic child at a laptop.

`hero-clase-en-directo.png`
